query AllCollections {
  collections {
    id
    name
  }
}

query GetCollection($colID: ID!) {
  collectionByID(id: $colID) {
    id
    name
    processSnapshots {
      id
      processID
    }
  }
}

mutation CollectCollection {
  collectCollection {
    id
  }
}
