type Mutation {
  createCollection(input: CreateCollectionInput): Collection
  collectCollection: Collection
}

extend type Query {
  collectionByID(id: ID!): Collection
  availableVars(func: String!, pc: Int!): [VarInfo!]
}

type VarInfo {
  Name: String!
  Type: String!
  VarType: Int!
}
