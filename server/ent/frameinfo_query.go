// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"stacksviz/ent/frameinfo"
	"stacksviz/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FrameInfoQuery is the builder for querying FrameInfo entities.
type FrameInfoQuery struct {
	config
	ctx        *QueryContext
	order      []frameinfo.OrderOption
	inters     []Interceptor
	predicates []predicate.FrameInfo
	withFKs    bool
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*FrameInfo) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FrameInfoQuery builder.
func (fiq *FrameInfoQuery) Where(ps ...predicate.FrameInfo) *FrameInfoQuery {
	fiq.predicates = append(fiq.predicates, ps...)
	return fiq
}

// Limit the number of records to be returned by this query.
func (fiq *FrameInfoQuery) Limit(limit int) *FrameInfoQuery {
	fiq.ctx.Limit = &limit
	return fiq
}

// Offset to start from.
func (fiq *FrameInfoQuery) Offset(offset int) *FrameInfoQuery {
	fiq.ctx.Offset = &offset
	return fiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fiq *FrameInfoQuery) Unique(unique bool) *FrameInfoQuery {
	fiq.ctx.Unique = &unique
	return fiq
}

// Order specifies how the records should be ordered.
func (fiq *FrameInfoQuery) Order(o ...frameinfo.OrderOption) *FrameInfoQuery {
	fiq.order = append(fiq.order, o...)
	return fiq
}

// First returns the first FrameInfo entity from the query.
// Returns a *NotFoundError when no FrameInfo was found.
func (fiq *FrameInfoQuery) First(ctx context.Context) (*FrameInfo, error) {
	nodes, err := fiq.Limit(1).All(setContextOp(ctx, fiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{frameinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fiq *FrameInfoQuery) FirstX(ctx context.Context) *FrameInfo {
	node, err := fiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FrameInfo ID from the query.
// Returns a *NotFoundError when no FrameInfo ID was found.
func (fiq *FrameInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fiq.Limit(1).IDs(setContextOp(ctx, fiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{frameinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fiq *FrameInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := fiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FrameInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FrameInfo entity is found.
// Returns a *NotFoundError when no FrameInfo entities are found.
func (fiq *FrameInfoQuery) Only(ctx context.Context) (*FrameInfo, error) {
	nodes, err := fiq.Limit(2).All(setContextOp(ctx, fiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{frameinfo.Label}
	default:
		return nil, &NotSingularError{frameinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fiq *FrameInfoQuery) OnlyX(ctx context.Context) *FrameInfo {
	node, err := fiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FrameInfo ID in the query.
// Returns a *NotSingularError when more than one FrameInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (fiq *FrameInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fiq.Limit(2).IDs(setContextOp(ctx, fiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{frameinfo.Label}
	default:
		err = &NotSingularError{frameinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fiq *FrameInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := fiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FrameInfos.
func (fiq *FrameInfoQuery) All(ctx context.Context) ([]*FrameInfo, error) {
	ctx = setContextOp(ctx, fiq.ctx, "All")
	if err := fiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FrameInfo, *FrameInfoQuery]()
	return withInterceptors[[]*FrameInfo](ctx, fiq, qr, fiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fiq *FrameInfoQuery) AllX(ctx context.Context) []*FrameInfo {
	nodes, err := fiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FrameInfo IDs.
func (fiq *FrameInfoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fiq.ctx.Unique == nil && fiq.path != nil {
		fiq.Unique(true)
	}
	ctx = setContextOp(ctx, fiq.ctx, "IDs")
	if err = fiq.Select(frameinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fiq *FrameInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := fiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fiq *FrameInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Count")
	if err := fiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fiq, querierCount[*FrameInfoQuery](), fiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fiq *FrameInfoQuery) CountX(ctx context.Context) int {
	count, err := fiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fiq *FrameInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Exist")
	switch _, err := fiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fiq *FrameInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := fiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FrameInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fiq *FrameInfoQuery) Clone() *FrameInfoQuery {
	if fiq == nil {
		return nil
	}
	return &FrameInfoQuery{
		config:     fiq.config,
		ctx:        fiq.ctx.Clone(),
		order:      append([]frameinfo.OrderOption{}, fiq.order...),
		inters:     append([]Interceptor{}, fiq.inters...),
		predicates: append([]predicate.FrameInfo{}, fiq.predicates...),
		// clone intermediate query.
		sql:  fiq.sql.Clone(),
		path: fiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Frame string `json:"frame,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FrameInfo.Query().
//		GroupBy(frameinfo.FieldFrame).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fiq *FrameInfoQuery) GroupBy(field string, fields ...string) *FrameInfoGroupBy {
	fiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FrameInfoGroupBy{build: fiq}
	grbuild.flds = &fiq.ctx.Fields
	grbuild.label = frameinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Frame string `json:"frame,omitempty"`
//	}
//
//	client.FrameInfo.Query().
//		Select(frameinfo.FieldFrame).
//		Scan(ctx, &v)
func (fiq *FrameInfoQuery) Select(fields ...string) *FrameInfoSelect {
	fiq.ctx.Fields = append(fiq.ctx.Fields, fields...)
	sbuild := &FrameInfoSelect{FrameInfoQuery: fiq}
	sbuild.label = frameinfo.Label
	sbuild.flds, sbuild.scan = &fiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FrameInfoSelect configured with the given aggregations.
func (fiq *FrameInfoQuery) Aggregate(fns ...AggregateFunc) *FrameInfoSelect {
	return fiq.Select().Aggregate(fns...)
}

func (fiq *FrameInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fiq); err != nil {
				return err
			}
		}
	}
	for _, f := range fiq.ctx.Fields {
		if !frameinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fiq.path != nil {
		prev, err := fiq.path(ctx)
		if err != nil {
			return err
		}
		fiq.sql = prev
	}
	return nil
}

func (fiq *FrameInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FrameInfo, error) {
	var (
		nodes   = []*FrameInfo{}
		withFKs = fiq.withFKs
		_spec   = fiq.querySpec()
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, frameinfo.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FrameInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FrameInfo{config: fiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(fiq.modifiers) > 0 {
		_spec.Modifiers = fiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range fiq.loadTotal {
		if err := fiq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fiq *FrameInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fiq.querySpec()
	if len(fiq.modifiers) > 0 {
		_spec.Modifiers = fiq.modifiers
	}
	_spec.Node.Columns = fiq.ctx.Fields
	if len(fiq.ctx.Fields) > 0 {
		_spec.Unique = fiq.ctx.Unique != nil && *fiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fiq.driver, _spec)
}

func (fiq *FrameInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(frameinfo.Table, frameinfo.Columns, sqlgraph.NewFieldSpec(frameinfo.FieldID, field.TypeInt))
	_spec.From = fiq.sql
	if unique := fiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fiq.path != nil {
		_spec.Unique = true
	}
	if fields := fiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, frameinfo.FieldID)
		for i := range fields {
			if fields[i] != frameinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fiq *FrameInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fiq.driver.Dialect())
	t1 := builder.Table(frameinfo.Table)
	columns := fiq.ctx.Fields
	if len(columns) == 0 {
		columns = frameinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fiq.sql != nil {
		selector = fiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fiq.ctx.Unique != nil && *fiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fiq.predicates {
		p(selector)
	}
	for _, p := range fiq.order {
		p(selector)
	}
	if offset := fiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FrameInfoGroupBy is the group-by builder for FrameInfo entities.
type FrameInfoGroupBy struct {
	selector
	build *FrameInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (figb *FrameInfoGroupBy) Aggregate(fns ...AggregateFunc) *FrameInfoGroupBy {
	figb.fns = append(figb.fns, fns...)
	return figb
}

// Scan applies the selector query and scans the result into the given value.
func (figb *FrameInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, figb.build.ctx, "GroupBy")
	if err := figb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FrameInfoQuery, *FrameInfoGroupBy](ctx, figb.build, figb, figb.build.inters, v)
}

func (figb *FrameInfoGroupBy) sqlScan(ctx context.Context, root *FrameInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(figb.fns))
	for _, fn := range figb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*figb.flds)+len(figb.fns))
		for _, f := range *figb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*figb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := figb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FrameInfoSelect is the builder for selecting fields of FrameInfo entities.
type FrameInfoSelect struct {
	*FrameInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fis *FrameInfoSelect) Aggregate(fns ...AggregateFunc) *FrameInfoSelect {
	fis.fns = append(fis.fns, fns...)
	return fis
}

// Scan applies the selector query and scans the result into the given value.
func (fis *FrameInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fis.ctx, "Select")
	if err := fis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FrameInfoQuery, *FrameInfoSelect](ctx, fis.FrameInfoQuery, fis, fis.inters, v)
}

func (fis *FrameInfoSelect) sqlScan(ctx context.Context, root *FrameInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fis.fns))
	for _, fn := range fis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
