// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"stacksviz/ent/collection"
	"stacksviz/ent/collectspec"
	"stacksviz/ent/framespec"
	"stacksviz/ent/processsnapshot"

	"entgo.io/contrib/entgql"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/99designs/gqlgen/graphql"
	"github.com/99designs/gqlgen/graphql/errcode"
	"github.com/vektah/gqlparser/v2/gqlerror"
)

// Common entgql types.
type (
	Cursor         = entgql.Cursor[int]
	PageInfo       = entgql.PageInfo[int]
	OrderDirection = entgql.OrderDirection
)

func orderFunc(o OrderDirection, field string) func(*sql.Selector) {
	if o == entgql.OrderDirectionDesc {
		return Desc(field)
	}
	return Asc(field)
}

const errInvalidPagination = "INVALID_PAGINATION"

func validateFirstLast(first, last *int) (err *gqlerror.Error) {
	switch {
	case first != nil && last != nil:
		err = &gqlerror.Error{
			Message: "Passing both `first` and `last` to paginate a connection is not supported.",
		}
	case first != nil && *first < 0:
		err = &gqlerror.Error{
			Message: "`first` on a connection cannot be less than zero.",
		}
		errcode.Set(err, errInvalidPagination)
	case last != nil && *last < 0:
		err = &gqlerror.Error{
			Message: "`last` on a connection cannot be less than zero.",
		}
		errcode.Set(err, errInvalidPagination)
	}
	return err
}

func collectedField(ctx context.Context, path ...string) *graphql.CollectedField {
	fc := graphql.GetFieldContext(ctx)
	if fc == nil {
		return nil
	}
	field := fc.Field
	oc := graphql.GetOperationContext(ctx)
walk:
	for _, name := range path {
		for _, f := range graphql.CollectFields(oc, field.Selections, nil) {
			if f.Alias == name {
				field = f
				continue walk
			}
		}
		return nil
	}
	return &field
}

func hasCollectedField(ctx context.Context, path ...string) bool {
	if graphql.GetFieldContext(ctx) == nil {
		return true
	}
	return collectedField(ctx, path...) != nil
}

const (
	edgesField      = "edges"
	nodeField       = "node"
	pageInfoField   = "pageInfo"
	totalCountField = "totalCount"
)

func paginateLimit(first, last *int) int {
	var limit int
	if first != nil {
		limit = *first + 1
	} else if last != nil {
		limit = *last + 1
	}
	return limit
}

// CollectSpecEdge is the edge representation of CollectSpec.
type CollectSpecEdge struct {
	Node   *CollectSpec `json:"node"`
	Cursor Cursor       `json:"cursor"`
}

// CollectSpecConnection is the connection containing edges to CollectSpec.
type CollectSpecConnection struct {
	Edges      []*CollectSpecEdge `json:"edges"`
	PageInfo   PageInfo           `json:"pageInfo"`
	TotalCount int                `json:"totalCount"`
}

func (c *CollectSpecConnection) build(nodes []*CollectSpec, pager *collectspecPager, after *Cursor, first *int, before *Cursor, last *int) {
	c.PageInfo.HasNextPage = before != nil
	c.PageInfo.HasPreviousPage = after != nil
	if first != nil && *first+1 == len(nodes) {
		c.PageInfo.HasNextPage = true
		nodes = nodes[:len(nodes)-1]
	} else if last != nil && *last+1 == len(nodes) {
		c.PageInfo.HasPreviousPage = true
		nodes = nodes[:len(nodes)-1]
	}
	var nodeAt func(int) *CollectSpec
	if last != nil {
		n := len(nodes) - 1
		nodeAt = func(i int) *CollectSpec {
			return nodes[n-i]
		}
	} else {
		nodeAt = func(i int) *CollectSpec {
			return nodes[i]
		}
	}
	c.Edges = make([]*CollectSpecEdge, len(nodes))
	for i := range nodes {
		node := nodeAt(i)
		c.Edges[i] = &CollectSpecEdge{
			Node:   node,
			Cursor: pager.toCursor(node),
		}
	}
	if l := len(c.Edges); l > 0 {
		c.PageInfo.StartCursor = &c.Edges[0].Cursor
		c.PageInfo.EndCursor = &c.Edges[l-1].Cursor
	}
	if c.TotalCount == 0 {
		c.TotalCount = len(nodes)
	}
}

// CollectSpecPaginateOption enables pagination customization.
type CollectSpecPaginateOption func(*collectspecPager) error

// WithCollectSpecOrder configures pagination ordering.
func WithCollectSpecOrder(order *CollectSpecOrder) CollectSpecPaginateOption {
	if order == nil {
		order = DefaultCollectSpecOrder
	}
	o := *order
	return func(pager *collectspecPager) error {
		if err := o.Direction.Validate(); err != nil {
			return err
		}
		if o.Field == nil {
			o.Field = DefaultCollectSpecOrder.Field
		}
		pager.order = &o
		return nil
	}
}

// WithCollectSpecFilter configures pagination filter.
func WithCollectSpecFilter(filter func(*CollectSpecQuery) (*CollectSpecQuery, error)) CollectSpecPaginateOption {
	return func(pager *collectspecPager) error {
		if filter == nil {
			return errors.New("CollectSpecQuery filter cannot be nil")
		}
		pager.filter = filter
		return nil
	}
}

type collectspecPager struct {
	reverse bool
	order   *CollectSpecOrder
	filter  func(*CollectSpecQuery) (*CollectSpecQuery, error)
}

func newCollectSpecPager(opts []CollectSpecPaginateOption, reverse bool) (*collectspecPager, error) {
	pager := &collectspecPager{reverse: reverse}
	for _, opt := range opts {
		if err := opt(pager); err != nil {
			return nil, err
		}
	}
	if pager.order == nil {
		pager.order = DefaultCollectSpecOrder
	}
	return pager, nil
}

func (p *collectspecPager) applyFilter(query *CollectSpecQuery) (*CollectSpecQuery, error) {
	if p.filter != nil {
		return p.filter(query)
	}
	return query, nil
}

func (p *collectspecPager) toCursor(cs *CollectSpec) Cursor {
	return p.order.Field.toCursor(cs)
}

func (p *collectspecPager) applyCursors(query *CollectSpecQuery, after, before *Cursor) (*CollectSpecQuery, error) {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	for _, predicate := range entgql.CursorsPredicate(after, before, DefaultCollectSpecOrder.Field.column, p.order.Field.column, direction) {
		query = query.Where(predicate)
	}
	return query, nil
}

func (p *collectspecPager) applyOrder(query *CollectSpecQuery) *CollectSpecQuery {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	query = query.Order(p.order.Field.toTerm(direction.OrderTermOption()))
	if p.order.Field != DefaultCollectSpecOrder.Field {
		query = query.Order(DefaultCollectSpecOrder.Field.toTerm(direction.OrderTermOption()))
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return query
}

func (p *collectspecPager) orderExpr(query *CollectSpecQuery) sql.Querier {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return sql.ExprFunc(func(b *sql.Builder) {
		b.Ident(p.order.Field.column).Pad().WriteString(string(direction))
		if p.order.Field != DefaultCollectSpecOrder.Field {
			b.Comma().Ident(DefaultCollectSpecOrder.Field.column).Pad().WriteString(string(direction))
		}
	})
}

// Paginate executes the query and returns a relay based cursor connection to CollectSpec.
func (cs *CollectSpecQuery) Paginate(
	ctx context.Context, after *Cursor, first *int,
	before *Cursor, last *int, opts ...CollectSpecPaginateOption,
) (*CollectSpecConnection, error) {
	if err := validateFirstLast(first, last); err != nil {
		return nil, err
	}
	pager, err := newCollectSpecPager(opts, last != nil)
	if err != nil {
		return nil, err
	}
	if cs, err = pager.applyFilter(cs); err != nil {
		return nil, err
	}
	conn := &CollectSpecConnection{Edges: []*CollectSpecEdge{}}
	ignoredEdges := !hasCollectedField(ctx, edgesField)
	if hasCollectedField(ctx, totalCountField) || hasCollectedField(ctx, pageInfoField) {
		hasPagination := after != nil || first != nil || before != nil || last != nil
		if hasPagination || ignoredEdges {
			if conn.TotalCount, err = cs.Clone().Count(ctx); err != nil {
				return nil, err
			}
			conn.PageInfo.HasNextPage = first != nil && conn.TotalCount > 0
			conn.PageInfo.HasPreviousPage = last != nil && conn.TotalCount > 0
		}
	}
	if ignoredEdges || (first != nil && *first == 0) || (last != nil && *last == 0) {
		return conn, nil
	}
	if cs, err = pager.applyCursors(cs, after, before); err != nil {
		return nil, err
	}
	if limit := paginateLimit(first, last); limit != 0 {
		cs.Limit(limit)
	}
	if field := collectedField(ctx, edgesField, nodeField); field != nil {
		if err := cs.collectField(ctx, graphql.GetOperationContext(ctx), *field, []string{edgesField, nodeField}); err != nil {
			return nil, err
		}
	}
	cs = pager.applyOrder(cs)
	nodes, err := cs.All(ctx)
	if err != nil {
		return nil, err
	}
	conn.build(nodes, pager, after, first, before, last)
	return conn, nil
}

// CollectSpecOrderField defines the ordering field of CollectSpec.
type CollectSpecOrderField struct {
	// Value extracts the ordering value from the given CollectSpec.
	Value    func(*CollectSpec) (ent.Value, error)
	column   string // field or computed.
	toTerm   func(...sql.OrderTermOption) collectspec.OrderOption
	toCursor func(*CollectSpec) Cursor
}

// CollectSpecOrder defines the ordering of CollectSpec.
type CollectSpecOrder struct {
	Direction OrderDirection         `json:"direction"`
	Field     *CollectSpecOrderField `json:"field"`
}

// DefaultCollectSpecOrder is the default ordering of CollectSpec.
var DefaultCollectSpecOrder = &CollectSpecOrder{
	Direction: entgql.OrderDirectionAsc,
	Field: &CollectSpecOrderField{
		Value: func(cs *CollectSpec) (ent.Value, error) {
			return cs.ID, nil
		},
		column: collectspec.FieldID,
		toTerm: collectspec.ByID,
		toCursor: func(cs *CollectSpec) Cursor {
			return Cursor{ID: cs.ID}
		},
	},
}

// ToEdge converts CollectSpec into CollectSpecEdge.
func (cs *CollectSpec) ToEdge(order *CollectSpecOrder) *CollectSpecEdge {
	if order == nil {
		order = DefaultCollectSpecOrder
	}
	return &CollectSpecEdge{
		Node:   cs,
		Cursor: order.Field.toCursor(cs),
	}
}

// CollectionEdge is the edge representation of Collection.
type CollectionEdge struct {
	Node   *Collection `json:"node"`
	Cursor Cursor      `json:"cursor"`
}

// CollectionConnection is the connection containing edges to Collection.
type CollectionConnection struct {
	Edges      []*CollectionEdge `json:"edges"`
	PageInfo   PageInfo          `json:"pageInfo"`
	TotalCount int               `json:"totalCount"`
}

func (c *CollectionConnection) build(nodes []*Collection, pager *collectionPager, after *Cursor, first *int, before *Cursor, last *int) {
	c.PageInfo.HasNextPage = before != nil
	c.PageInfo.HasPreviousPage = after != nil
	if first != nil && *first+1 == len(nodes) {
		c.PageInfo.HasNextPage = true
		nodes = nodes[:len(nodes)-1]
	} else if last != nil && *last+1 == len(nodes) {
		c.PageInfo.HasPreviousPage = true
		nodes = nodes[:len(nodes)-1]
	}
	var nodeAt func(int) *Collection
	if last != nil {
		n := len(nodes) - 1
		nodeAt = func(i int) *Collection {
			return nodes[n-i]
		}
	} else {
		nodeAt = func(i int) *Collection {
			return nodes[i]
		}
	}
	c.Edges = make([]*CollectionEdge, len(nodes))
	for i := range nodes {
		node := nodeAt(i)
		c.Edges[i] = &CollectionEdge{
			Node:   node,
			Cursor: pager.toCursor(node),
		}
	}
	if l := len(c.Edges); l > 0 {
		c.PageInfo.StartCursor = &c.Edges[0].Cursor
		c.PageInfo.EndCursor = &c.Edges[l-1].Cursor
	}
	if c.TotalCount == 0 {
		c.TotalCount = len(nodes)
	}
}

// CollectionPaginateOption enables pagination customization.
type CollectionPaginateOption func(*collectionPager) error

// WithCollectionOrder configures pagination ordering.
func WithCollectionOrder(order *CollectionOrder) CollectionPaginateOption {
	if order == nil {
		order = DefaultCollectionOrder
	}
	o := *order
	return func(pager *collectionPager) error {
		if err := o.Direction.Validate(); err != nil {
			return err
		}
		if o.Field == nil {
			o.Field = DefaultCollectionOrder.Field
		}
		pager.order = &o
		return nil
	}
}

// WithCollectionFilter configures pagination filter.
func WithCollectionFilter(filter func(*CollectionQuery) (*CollectionQuery, error)) CollectionPaginateOption {
	return func(pager *collectionPager) error {
		if filter == nil {
			return errors.New("CollectionQuery filter cannot be nil")
		}
		pager.filter = filter
		return nil
	}
}

type collectionPager struct {
	reverse bool
	order   *CollectionOrder
	filter  func(*CollectionQuery) (*CollectionQuery, error)
}

func newCollectionPager(opts []CollectionPaginateOption, reverse bool) (*collectionPager, error) {
	pager := &collectionPager{reverse: reverse}
	for _, opt := range opts {
		if err := opt(pager); err != nil {
			return nil, err
		}
	}
	if pager.order == nil {
		pager.order = DefaultCollectionOrder
	}
	return pager, nil
}

func (p *collectionPager) applyFilter(query *CollectionQuery) (*CollectionQuery, error) {
	if p.filter != nil {
		return p.filter(query)
	}
	return query, nil
}

func (p *collectionPager) toCursor(c *Collection) Cursor {
	return p.order.Field.toCursor(c)
}

func (p *collectionPager) applyCursors(query *CollectionQuery, after, before *Cursor) (*CollectionQuery, error) {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	for _, predicate := range entgql.CursorsPredicate(after, before, DefaultCollectionOrder.Field.column, p.order.Field.column, direction) {
		query = query.Where(predicate)
	}
	return query, nil
}

func (p *collectionPager) applyOrder(query *CollectionQuery) *CollectionQuery {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	query = query.Order(p.order.Field.toTerm(direction.OrderTermOption()))
	if p.order.Field != DefaultCollectionOrder.Field {
		query = query.Order(DefaultCollectionOrder.Field.toTerm(direction.OrderTermOption()))
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return query
}

func (p *collectionPager) orderExpr(query *CollectionQuery) sql.Querier {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return sql.ExprFunc(func(b *sql.Builder) {
		b.Ident(p.order.Field.column).Pad().WriteString(string(direction))
		if p.order.Field != DefaultCollectionOrder.Field {
			b.Comma().Ident(DefaultCollectionOrder.Field.column).Pad().WriteString(string(direction))
		}
	})
}

// Paginate executes the query and returns a relay based cursor connection to Collection.
func (c *CollectionQuery) Paginate(
	ctx context.Context, after *Cursor, first *int,
	before *Cursor, last *int, opts ...CollectionPaginateOption,
) (*CollectionConnection, error) {
	if err := validateFirstLast(first, last); err != nil {
		return nil, err
	}
	pager, err := newCollectionPager(opts, last != nil)
	if err != nil {
		return nil, err
	}
	if c, err = pager.applyFilter(c); err != nil {
		return nil, err
	}
	conn := &CollectionConnection{Edges: []*CollectionEdge{}}
	ignoredEdges := !hasCollectedField(ctx, edgesField)
	if hasCollectedField(ctx, totalCountField) || hasCollectedField(ctx, pageInfoField) {
		hasPagination := after != nil || first != nil || before != nil || last != nil
		if hasPagination || ignoredEdges {
			if conn.TotalCount, err = c.Clone().Count(ctx); err != nil {
				return nil, err
			}
			conn.PageInfo.HasNextPage = first != nil && conn.TotalCount > 0
			conn.PageInfo.HasPreviousPage = last != nil && conn.TotalCount > 0
		}
	}
	if ignoredEdges || (first != nil && *first == 0) || (last != nil && *last == 0) {
		return conn, nil
	}
	if c, err = pager.applyCursors(c, after, before); err != nil {
		return nil, err
	}
	if limit := paginateLimit(first, last); limit != 0 {
		c.Limit(limit)
	}
	if field := collectedField(ctx, edgesField, nodeField); field != nil {
		if err := c.collectField(ctx, graphql.GetOperationContext(ctx), *field, []string{edgesField, nodeField}); err != nil {
			return nil, err
		}
	}
	c = pager.applyOrder(c)
	nodes, err := c.All(ctx)
	if err != nil {
		return nil, err
	}
	conn.build(nodes, pager, after, first, before, last)
	return conn, nil
}

// CollectionOrderField defines the ordering field of Collection.
type CollectionOrderField struct {
	// Value extracts the ordering value from the given Collection.
	Value    func(*Collection) (ent.Value, error)
	column   string // field or computed.
	toTerm   func(...sql.OrderTermOption) collection.OrderOption
	toCursor func(*Collection) Cursor
}

// CollectionOrder defines the ordering of Collection.
type CollectionOrder struct {
	Direction OrderDirection        `json:"direction"`
	Field     *CollectionOrderField `json:"field"`
}

// DefaultCollectionOrder is the default ordering of Collection.
var DefaultCollectionOrder = &CollectionOrder{
	Direction: entgql.OrderDirectionAsc,
	Field: &CollectionOrderField{
		Value: func(c *Collection) (ent.Value, error) {
			return c.ID, nil
		},
		column: collection.FieldID,
		toTerm: collection.ByID,
		toCursor: func(c *Collection) Cursor {
			return Cursor{ID: c.ID}
		},
	},
}

// ToEdge converts Collection into CollectionEdge.
func (c *Collection) ToEdge(order *CollectionOrder) *CollectionEdge {
	if order == nil {
		order = DefaultCollectionOrder
	}
	return &CollectionEdge{
		Node:   c,
		Cursor: order.Field.toCursor(c),
	}
}

// FrameSpecEdge is the edge representation of FrameSpec.
type FrameSpecEdge struct {
	Node   *FrameSpec `json:"node"`
	Cursor Cursor     `json:"cursor"`
}

// FrameSpecConnection is the connection containing edges to FrameSpec.
type FrameSpecConnection struct {
	Edges      []*FrameSpecEdge `json:"edges"`
	PageInfo   PageInfo         `json:"pageInfo"`
	TotalCount int              `json:"totalCount"`
}

func (c *FrameSpecConnection) build(nodes []*FrameSpec, pager *framespecPager, after *Cursor, first *int, before *Cursor, last *int) {
	c.PageInfo.HasNextPage = before != nil
	c.PageInfo.HasPreviousPage = after != nil
	if first != nil && *first+1 == len(nodes) {
		c.PageInfo.HasNextPage = true
		nodes = nodes[:len(nodes)-1]
	} else if last != nil && *last+1 == len(nodes) {
		c.PageInfo.HasPreviousPage = true
		nodes = nodes[:len(nodes)-1]
	}
	var nodeAt func(int) *FrameSpec
	if last != nil {
		n := len(nodes) - 1
		nodeAt = func(i int) *FrameSpec {
			return nodes[n-i]
		}
	} else {
		nodeAt = func(i int) *FrameSpec {
			return nodes[i]
		}
	}
	c.Edges = make([]*FrameSpecEdge, len(nodes))
	for i := range nodes {
		node := nodeAt(i)
		c.Edges[i] = &FrameSpecEdge{
			Node:   node,
			Cursor: pager.toCursor(node),
		}
	}
	if l := len(c.Edges); l > 0 {
		c.PageInfo.StartCursor = &c.Edges[0].Cursor
		c.PageInfo.EndCursor = &c.Edges[l-1].Cursor
	}
	if c.TotalCount == 0 {
		c.TotalCount = len(nodes)
	}
}

// FrameSpecPaginateOption enables pagination customization.
type FrameSpecPaginateOption func(*framespecPager) error

// WithFrameSpecOrder configures pagination ordering.
func WithFrameSpecOrder(order *FrameSpecOrder) FrameSpecPaginateOption {
	if order == nil {
		order = DefaultFrameSpecOrder
	}
	o := *order
	return func(pager *framespecPager) error {
		if err := o.Direction.Validate(); err != nil {
			return err
		}
		if o.Field == nil {
			o.Field = DefaultFrameSpecOrder.Field
		}
		pager.order = &o
		return nil
	}
}

// WithFrameSpecFilter configures pagination filter.
func WithFrameSpecFilter(filter func(*FrameSpecQuery) (*FrameSpecQuery, error)) FrameSpecPaginateOption {
	return func(pager *framespecPager) error {
		if filter == nil {
			return errors.New("FrameSpecQuery filter cannot be nil")
		}
		pager.filter = filter
		return nil
	}
}

type framespecPager struct {
	reverse bool
	order   *FrameSpecOrder
	filter  func(*FrameSpecQuery) (*FrameSpecQuery, error)
}

func newFrameSpecPager(opts []FrameSpecPaginateOption, reverse bool) (*framespecPager, error) {
	pager := &framespecPager{reverse: reverse}
	for _, opt := range opts {
		if err := opt(pager); err != nil {
			return nil, err
		}
	}
	if pager.order == nil {
		pager.order = DefaultFrameSpecOrder
	}
	return pager, nil
}

func (p *framespecPager) applyFilter(query *FrameSpecQuery) (*FrameSpecQuery, error) {
	if p.filter != nil {
		return p.filter(query)
	}
	return query, nil
}

func (p *framespecPager) toCursor(fs *FrameSpec) Cursor {
	return p.order.Field.toCursor(fs)
}

func (p *framespecPager) applyCursors(query *FrameSpecQuery, after, before *Cursor) (*FrameSpecQuery, error) {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	for _, predicate := range entgql.CursorsPredicate(after, before, DefaultFrameSpecOrder.Field.column, p.order.Field.column, direction) {
		query = query.Where(predicate)
	}
	return query, nil
}

func (p *framespecPager) applyOrder(query *FrameSpecQuery) *FrameSpecQuery {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	query = query.Order(p.order.Field.toTerm(direction.OrderTermOption()))
	if p.order.Field != DefaultFrameSpecOrder.Field {
		query = query.Order(DefaultFrameSpecOrder.Field.toTerm(direction.OrderTermOption()))
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return query
}

func (p *framespecPager) orderExpr(query *FrameSpecQuery) sql.Querier {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return sql.ExprFunc(func(b *sql.Builder) {
		b.Ident(p.order.Field.column).Pad().WriteString(string(direction))
		if p.order.Field != DefaultFrameSpecOrder.Field {
			b.Comma().Ident(DefaultFrameSpecOrder.Field.column).Pad().WriteString(string(direction))
		}
	})
}

// Paginate executes the query and returns a relay based cursor connection to FrameSpec.
func (fs *FrameSpecQuery) Paginate(
	ctx context.Context, after *Cursor, first *int,
	before *Cursor, last *int, opts ...FrameSpecPaginateOption,
) (*FrameSpecConnection, error) {
	if err := validateFirstLast(first, last); err != nil {
		return nil, err
	}
	pager, err := newFrameSpecPager(opts, last != nil)
	if err != nil {
		return nil, err
	}
	if fs, err = pager.applyFilter(fs); err != nil {
		return nil, err
	}
	conn := &FrameSpecConnection{Edges: []*FrameSpecEdge{}}
	ignoredEdges := !hasCollectedField(ctx, edgesField)
	if hasCollectedField(ctx, totalCountField) || hasCollectedField(ctx, pageInfoField) {
		hasPagination := after != nil || first != nil || before != nil || last != nil
		if hasPagination || ignoredEdges {
			if conn.TotalCount, err = fs.Clone().Count(ctx); err != nil {
				return nil, err
			}
			conn.PageInfo.HasNextPage = first != nil && conn.TotalCount > 0
			conn.PageInfo.HasPreviousPage = last != nil && conn.TotalCount > 0
		}
	}
	if ignoredEdges || (first != nil && *first == 0) || (last != nil && *last == 0) {
		return conn, nil
	}
	if fs, err = pager.applyCursors(fs, after, before); err != nil {
		return nil, err
	}
	if limit := paginateLimit(first, last); limit != 0 {
		fs.Limit(limit)
	}
	if field := collectedField(ctx, edgesField, nodeField); field != nil {
		if err := fs.collectField(ctx, graphql.GetOperationContext(ctx), *field, []string{edgesField, nodeField}); err != nil {
			return nil, err
		}
	}
	fs = pager.applyOrder(fs)
	nodes, err := fs.All(ctx)
	if err != nil {
		return nil, err
	}
	conn.build(nodes, pager, after, first, before, last)
	return conn, nil
}

// FrameSpecOrderField defines the ordering field of FrameSpec.
type FrameSpecOrderField struct {
	// Value extracts the ordering value from the given FrameSpec.
	Value    func(*FrameSpec) (ent.Value, error)
	column   string // field or computed.
	toTerm   func(...sql.OrderTermOption) framespec.OrderOption
	toCursor func(*FrameSpec) Cursor
}

// FrameSpecOrder defines the ordering of FrameSpec.
type FrameSpecOrder struct {
	Direction OrderDirection       `json:"direction"`
	Field     *FrameSpecOrderField `json:"field"`
}

// DefaultFrameSpecOrder is the default ordering of FrameSpec.
var DefaultFrameSpecOrder = &FrameSpecOrder{
	Direction: entgql.OrderDirectionAsc,
	Field: &FrameSpecOrderField{
		Value: func(fs *FrameSpec) (ent.Value, error) {
			return fs.ID, nil
		},
		column: framespec.FieldID,
		toTerm: framespec.ByID,
		toCursor: func(fs *FrameSpec) Cursor {
			return Cursor{ID: fs.ID}
		},
	},
}

// ToEdge converts FrameSpec into FrameSpecEdge.
func (fs *FrameSpec) ToEdge(order *FrameSpecOrder) *FrameSpecEdge {
	if order == nil {
		order = DefaultFrameSpecOrder
	}
	return &FrameSpecEdge{
		Node:   fs,
		Cursor: order.Field.toCursor(fs),
	}
}

// ProcessSnapshotEdge is the edge representation of ProcessSnapshot.
type ProcessSnapshotEdge struct {
	Node   *ProcessSnapshot `json:"node"`
	Cursor Cursor           `json:"cursor"`
}

// ProcessSnapshotConnection is the connection containing edges to ProcessSnapshot.
type ProcessSnapshotConnection struct {
	Edges      []*ProcessSnapshotEdge `json:"edges"`
	PageInfo   PageInfo               `json:"pageInfo"`
	TotalCount int                    `json:"totalCount"`
}

func (c *ProcessSnapshotConnection) build(nodes []*ProcessSnapshot, pager *processsnapshotPager, after *Cursor, first *int, before *Cursor, last *int) {
	c.PageInfo.HasNextPage = before != nil
	c.PageInfo.HasPreviousPage = after != nil
	if first != nil && *first+1 == len(nodes) {
		c.PageInfo.HasNextPage = true
		nodes = nodes[:len(nodes)-1]
	} else if last != nil && *last+1 == len(nodes) {
		c.PageInfo.HasPreviousPage = true
		nodes = nodes[:len(nodes)-1]
	}
	var nodeAt func(int) *ProcessSnapshot
	if last != nil {
		n := len(nodes) - 1
		nodeAt = func(i int) *ProcessSnapshot {
			return nodes[n-i]
		}
	} else {
		nodeAt = func(i int) *ProcessSnapshot {
			return nodes[i]
		}
	}
	c.Edges = make([]*ProcessSnapshotEdge, len(nodes))
	for i := range nodes {
		node := nodeAt(i)
		c.Edges[i] = &ProcessSnapshotEdge{
			Node:   node,
			Cursor: pager.toCursor(node),
		}
	}
	if l := len(c.Edges); l > 0 {
		c.PageInfo.StartCursor = &c.Edges[0].Cursor
		c.PageInfo.EndCursor = &c.Edges[l-1].Cursor
	}
	if c.TotalCount == 0 {
		c.TotalCount = len(nodes)
	}
}

// ProcessSnapshotPaginateOption enables pagination customization.
type ProcessSnapshotPaginateOption func(*processsnapshotPager) error

// WithProcessSnapshotOrder configures pagination ordering.
func WithProcessSnapshotOrder(order *ProcessSnapshotOrder) ProcessSnapshotPaginateOption {
	if order == nil {
		order = DefaultProcessSnapshotOrder
	}
	o := *order
	return func(pager *processsnapshotPager) error {
		if err := o.Direction.Validate(); err != nil {
			return err
		}
		if o.Field == nil {
			o.Field = DefaultProcessSnapshotOrder.Field
		}
		pager.order = &o
		return nil
	}
}

// WithProcessSnapshotFilter configures pagination filter.
func WithProcessSnapshotFilter(filter func(*ProcessSnapshotQuery) (*ProcessSnapshotQuery, error)) ProcessSnapshotPaginateOption {
	return func(pager *processsnapshotPager) error {
		if filter == nil {
			return errors.New("ProcessSnapshotQuery filter cannot be nil")
		}
		pager.filter = filter
		return nil
	}
}

type processsnapshotPager struct {
	reverse bool
	order   *ProcessSnapshotOrder
	filter  func(*ProcessSnapshotQuery) (*ProcessSnapshotQuery, error)
}

func newProcessSnapshotPager(opts []ProcessSnapshotPaginateOption, reverse bool) (*processsnapshotPager, error) {
	pager := &processsnapshotPager{reverse: reverse}
	for _, opt := range opts {
		if err := opt(pager); err != nil {
			return nil, err
		}
	}
	if pager.order == nil {
		pager.order = DefaultProcessSnapshotOrder
	}
	return pager, nil
}

func (p *processsnapshotPager) applyFilter(query *ProcessSnapshotQuery) (*ProcessSnapshotQuery, error) {
	if p.filter != nil {
		return p.filter(query)
	}
	return query, nil
}

func (p *processsnapshotPager) toCursor(ps *ProcessSnapshot) Cursor {
	return p.order.Field.toCursor(ps)
}

func (p *processsnapshotPager) applyCursors(query *ProcessSnapshotQuery, after, before *Cursor) (*ProcessSnapshotQuery, error) {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	for _, predicate := range entgql.CursorsPredicate(after, before, DefaultProcessSnapshotOrder.Field.column, p.order.Field.column, direction) {
		query = query.Where(predicate)
	}
	return query, nil
}

func (p *processsnapshotPager) applyOrder(query *ProcessSnapshotQuery) *ProcessSnapshotQuery {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	query = query.Order(p.order.Field.toTerm(direction.OrderTermOption()))
	if p.order.Field != DefaultProcessSnapshotOrder.Field {
		query = query.Order(DefaultProcessSnapshotOrder.Field.toTerm(direction.OrderTermOption()))
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return query
}

func (p *processsnapshotPager) orderExpr(query *ProcessSnapshotQuery) sql.Querier {
	direction := p.order.Direction
	if p.reverse {
		direction = direction.Reverse()
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(p.order.Field.column)
	}
	return sql.ExprFunc(func(b *sql.Builder) {
		b.Ident(p.order.Field.column).Pad().WriteString(string(direction))
		if p.order.Field != DefaultProcessSnapshotOrder.Field {
			b.Comma().Ident(DefaultProcessSnapshotOrder.Field.column).Pad().WriteString(string(direction))
		}
	})
}

// Paginate executes the query and returns a relay based cursor connection to ProcessSnapshot.
func (ps *ProcessSnapshotQuery) Paginate(
	ctx context.Context, after *Cursor, first *int,
	before *Cursor, last *int, opts ...ProcessSnapshotPaginateOption,
) (*ProcessSnapshotConnection, error) {
	if err := validateFirstLast(first, last); err != nil {
		return nil, err
	}
	pager, err := newProcessSnapshotPager(opts, last != nil)
	if err != nil {
		return nil, err
	}
	if ps, err = pager.applyFilter(ps); err != nil {
		return nil, err
	}
	conn := &ProcessSnapshotConnection{Edges: []*ProcessSnapshotEdge{}}
	ignoredEdges := !hasCollectedField(ctx, edgesField)
	if hasCollectedField(ctx, totalCountField) || hasCollectedField(ctx, pageInfoField) {
		hasPagination := after != nil || first != nil || before != nil || last != nil
		if hasPagination || ignoredEdges {
			if conn.TotalCount, err = ps.Clone().Count(ctx); err != nil {
				return nil, err
			}
			conn.PageInfo.HasNextPage = first != nil && conn.TotalCount > 0
			conn.PageInfo.HasPreviousPage = last != nil && conn.TotalCount > 0
		}
	}
	if ignoredEdges || (first != nil && *first == 0) || (last != nil && *last == 0) {
		return conn, nil
	}
	if ps, err = pager.applyCursors(ps, after, before); err != nil {
		return nil, err
	}
	if limit := paginateLimit(first, last); limit != 0 {
		ps.Limit(limit)
	}
	if field := collectedField(ctx, edgesField, nodeField); field != nil {
		if err := ps.collectField(ctx, graphql.GetOperationContext(ctx), *field, []string{edgesField, nodeField}); err != nil {
			return nil, err
		}
	}
	ps = pager.applyOrder(ps)
	nodes, err := ps.All(ctx)
	if err != nil {
		return nil, err
	}
	conn.build(nodes, pager, after, first, before, last)
	return conn, nil
}

// ProcessSnapshotOrderField defines the ordering field of ProcessSnapshot.
type ProcessSnapshotOrderField struct {
	// Value extracts the ordering value from the given ProcessSnapshot.
	Value    func(*ProcessSnapshot) (ent.Value, error)
	column   string // field or computed.
	toTerm   func(...sql.OrderTermOption) processsnapshot.OrderOption
	toCursor func(*ProcessSnapshot) Cursor
}

// ProcessSnapshotOrder defines the ordering of ProcessSnapshot.
type ProcessSnapshotOrder struct {
	Direction OrderDirection             `json:"direction"`
	Field     *ProcessSnapshotOrderField `json:"field"`
}

// DefaultProcessSnapshotOrder is the default ordering of ProcessSnapshot.
var DefaultProcessSnapshotOrder = &ProcessSnapshotOrder{
	Direction: entgql.OrderDirectionAsc,
	Field: &ProcessSnapshotOrderField{
		Value: func(ps *ProcessSnapshot) (ent.Value, error) {
			return ps.ID, nil
		},
		column: processsnapshot.FieldID,
		toTerm: processsnapshot.ByID,
		toCursor: func(ps *ProcessSnapshot) Cursor {
			return Cursor{ID: ps.ID}
		},
	},
}

// ToEdge converts ProcessSnapshot into ProcessSnapshotEdge.
func (ps *ProcessSnapshot) ToEdge(order *ProcessSnapshotOrder) *ProcessSnapshotEdge {
	if order == nil {
		order = DefaultProcessSnapshotOrder
	}
	return &ProcessSnapshotEdge{
		Node:   ps,
		Cursor: order.Field.toCursor(ps),
	}
}
